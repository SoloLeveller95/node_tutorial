// NodeJS basics

// Enter the commands in CMD
// node -v >>> To check node version in your computer
// node >>> TO enter REPL

// Enter the commands in VScode Terminal
// node <filename> >>> To run node in the terminal

const amount = 6;

if (amount < 12) {
	console.log("Small number");
} else {
	console.log("Large number");
}
